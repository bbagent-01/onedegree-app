# S10.4 — Listings Schema Audit

> **Read-only audit.** Zero code changes. Output of S10.4; locked column spec consumed by S10.5 (migration 045).
> Branch: `track-b/1db-overlay` · Date: 2026-04-27 · Live DB queried via Supabase Management API.

## TL;DR (5 bullets)

1. **Real listings schema = 41 columns.** No `listing_type`, `max_guests`, `bedrooms`, `beds`, `bathrooms`, `weekly_discount`, `monthly_discount`, `cleaning_fee` writes from wizard, or any structured address column. All of those wizard fields get **stuffed into `description` as a `<!--meta:{...}-->` JSON HTML-comment blob** by `src/lib/listing-meta.ts`.
2. **Wizard collects ~50 fields across 9 steps; only ~15 reach a real DB column.** Most extras either land in the `description` meta-blob or in `access_settings.preview_content` (the per-section toggles JSONB sub-object).
3. **`access_settings` column DEFAULT is stale.** Default still has the pre-mig-020 6-key legacy shape (`see_full`/`message`/`request_book`/`request_intro`/`view_host_profile`); runtime writes the post-020 4-key shape (`see_preview`/`full_listing_contact`/`allow_intro_requests`/`preview_content`). Any insert that omits `access_settings` lands the wrong shape on a fresh row.
4. **`property_type` CHECK only allows 4 values** (`apartment`/`house`/`room`/`other`), but the wizard offers 6 labels (House/Apartment/Condo/Townhouse/Cabin/Other). `propertyTypeToDb()` collapses Condo→apartment, Townhouse/Cabin→house. The original UI distinction is preserved only inside the meta-blob (`meta.propertyLabel`).
5. **Several legacy/orphan columns confirmed:** `price_per_night`, `address`, `preview_photos`, `min_trust_gate`, `availability_start`, `availability_end`, `availability_flexible`, plus the four legacy access columns (`preview_visibility`, `full_visibility`, `min_trust_score`, `specific_user_ids`). All are still hard-coded by the wizard with throwaway values, but they have no real readers in active code.

---

## 1. Real schema (live DB, 41 columns)

Source: `https://api.supabase.com/v1/projects/ldoueidykjeglqndbaev/database/query` against `information_schema.columns` + `pg_constraint` + `pg_indexes` on **2026-04-27**. This is the ground truth — supersedes any prior dump.

### Columns

| # | Column | Type | Null? | Default | Notes |
|---|---|---|---|---|---|
| 1 | `id` | `uuid` | NO | `gen_random_uuid()` | PK |
| 2 | `host_id` | `uuid` | YES | — | FK→users (no FK constraint visible in dump but referenced by code) |
| 3 | `title` | `text` | NO | — | |
| 4 | `description` | `text` | YES | — | **Holds `<!--meta:{...}-->` JSON blob with guests/bedrooms/beds/bathrooms/address/cleaningFee/etc.** |
| 5 | `area_name` | `text` | YES | — | Neighborhood / city blurb shown publicly |
| 6 | `address` | `text` | YES | — | **ORPHAN** — never read, never written |
| 7 | `price_per_night` | `numeric` | YES | — | **ORPHAN** — never written; price lives in `price_min`/`price_max` |
| 8 | `preview_visibility` | `text` | YES | `'anyone'::text` | **LEGACY** — CHECK constraint, hardcoded `"anyone"` by wizard, only consumed by dead `src/lib/listing-data.ts` functions |
| 9 | `full_visibility` | `text` | YES | `'vouched'::text` | **LEGACY** — same as above, hardcoded `"anyone"` by wizard |
| 10 | `min_trust_score` | `integer` | YES | `0` | **LEGACY** — hardcoded `0` by wizard, dead reader |
| 11 | `house_rules` | `text` | YES | — | Wizard joins selected booleans + custom rules into `\n`-separated text |
| 12 | `amenities` | `text[]` | YES | — | Free-form text array; UI offers a fixed checklist of 16 |
| 13 | `availability_start` | `date` | YES | — | **ORPHAN** — never read by UI; real availability lives in `listing_availability` table |
| 14 | `availability_end` | `date` | YES | — | **ORPHAN** — same |
| 15 | `is_active` | `boolean` | YES | `true` | Used (delete = soft-toggle? actually DELETE removes the row) |
| 16 | `created_at` | `timestamptz` | YES | `now()` | |
| 17 | `property_type` | `text` | YES | — | CHECK: `apartment`/`house`/`room`/`other` only |
| 18 | `price_min` | `integer` | YES | — | Wizard sets price_min = price_max = nightly rate |
| 19 | `price_max` | `integer` | YES | — | Same |
| 20 | `availability_flexible` | `boolean` | YES | `false` | **ORPHAN** — write-only; no UI reader |
| 21 | `specific_user_ids` | `uuid[]` | YES | `'{}'::uuid[]` | **LEGACY** — only Track A code reads this; access now lives in `access_settings.see_preview.user_ids` |
| 22 | `updated_at` | `timestamptz` | YES | `now()` | Bumped manually in PATCH endpoints |
| 23 | `avg_listing_rating` | `numeric` | YES | `NULL` | Maintained by stay-confirmations rating triggers |
| 24 | `listing_review_count` | `integer` | YES | `0` | Same |
| 25 | `min_nights` | `integer` | YES | `1` | Set via `/api/listings/[id]/calendar-settings` PATCH |
| 26 | `max_nights` | `integer` | YES | `365` | Same — wizard never exposes |
| 27 | `prep_days` | `integer` | YES | `0` | Same |
| 28 | `advance_notice_days` | `integer` | YES | `1` | Same |
| 29 | `availability_window_months` | `integer` | YES | `12` | Same — wizard never exposes |
| 30 | `checkin_time` | `text` | YES | `'15:00'::text` | Calendar-settings PATCH |
| 31 | `checkout_time` | `text` | YES | `'11:00'::text` | Same |
| 32 | `blocked_checkin_days` | `text[]` | YES | `'{}'::text[]` | Calendar-settings PATCH; wizard never exposes |
| 33 | `blocked_checkout_days` | `text[]` | YES | `'{}'::text[]` | Same |
| 34 | `default_availability_status` | `text` | YES | — | CHECK: `available`/`possibly_available`/`blocked`. **Wizard collects `defaultAvailability` but stuffs it into meta-blob — column stays NULL on every wizard-created row.** |
| 35 | `min_trust_gate` | `integer` | NO | `0` | **ORPHAN** — readers exist (`browse-data`, `wishlist-data`, `listing-detail-data`, `trust-gate.tsx`, `gated-listing-dialog.tsx`) but **no writer anywhere**. Every row stays at 0. |
| 36 | `visibility_mode` | `text` | NO | `'preview_gated'::text` | CHECK: `public`/`preview_gated`/`hidden`. Wizard always sends `preview_gated` (presets in Step 8 don't change it). |
| 37 | `preview_photos` | `jsonb` | YES | — | **ORPHAN** — zero references in source; preview/cover photo state lives on `listing_photos.is_preview`/`is_cover` instead |
| 38 | `preview_description` | `text` | YES | — | Used; 200-char limit enforced in form |
| 39 | `access_settings` | `jsonb` | NO | **6-key legacy shape** (see §6) | Runtime writes 4-key shape; default mismatches |
| 40 | `cancellation_policy_override` | `jsonb` | YES | — | Live; managed via dedicated `/api/listings/[id]/cancellation-policy` route |
| 41 | `cleaning_fee` | `integer` | YES | — | **READ but NEVER WRITTEN by listing CRUD.** Read by listing detail page, messaging-data, booking flows. Wizard collects `cleaningFee` but routes it into the meta-blob, not the column. |

### CHECK constraints

```
listings_default_availability_status_check  default_availability_status IN ('available','possibly_available','blocked')
listings_full_visibility_check              full_visibility IN ('anyone','vouched','trusted','inner_circle','specific')
listings_preview_visibility_check           preview_visibility IN ('anyone','vouched','trusted','inner_circle','specific')
listings_property_type_check                property_type IN ('apartment','house','room','other')
listings_visibility_mode_check              visibility_mode IN ('public','preview_gated','hidden')
```

### Indexes

```
listings_pkey                  PRIMARY KEY (id)
idx_listings_active            (is_active) WHERE is_active = true
idx_listings_host              (host_id)
idx_listings_min_trust_gate    (min_trust_gate) WHERE min_trust_gate > 0   -- partial; effectively empty (orphan column)
```

### Related tables (referenced by wizard payload, listed for completeness)

`listing_photos` (13 cols): `id`, `listing_id`, `public_url`, `is_preview`, `sort_order`, `created_at`, `storage_path`, `is_cover`, `original_url`, `filter_preset`, `filter_settings`, `ai_enhanced`, `enhanced_url`. Filter fields added by mig 040.

`listing_availability` and `listing_access_grants` exist but are not touched by the create-listing wizard.

### Live data sanity-check

- Total rows: **61**
- `property_type` distribution: `apartment` 31 · `house` 22 · `room` 4 · `other` 4 (consistent with the 4-value CHECK)
- 100% of rows carry the post-020 4-key access_settings shape (`see_preview`, `full_listing_contact`, `allow_intro_requests`, `preview_content`).
- `cancellation_policy_override` is NULL on every row sampled.

---

## 2. Wizard field inventory (raw, by step)

`src/app/(app)/hosting/create/page.tsx` — 9 steps, single-component, all state in `WizardState`. State shape collected from `interface WizardState` (lines 64–123) and `publish()` (lines 423–545).

### Step 1 — Place & property type
| Field | Input | Required? | WizardState key |
|---|---|---|---|
| Place type | radio: Entire / Private / Shared | ✓ | `placeKind` |
| Property type | radio: House / Apartment / Condo / Townhouse / Cabin / Other | ✓ | `propertyLabel` |

### Step 2 — Address
| Field | Input | Required? | Key |
|---|---|---|---|
| Street | autocomplete-text | — | `street` |
| City | text | ✓ (`canAdvance`) | `city` |
| State | text | ✓ | `state` |
| ZIP | text | — | `zip` |
| Lat / Lng | derived from geocode + drag pin | — | `lat`, `lng` |
| Neighborhood / area_name | text | — | `areaName` (defaulted to `city, state` if empty) |

### Step 3 — Capacity & amenities
| Field | Input | Required? | Key |
|---|---|---|---|
| Maximum guests | counter ≥1 | ✓ | `guests` |
| Bedrooms | counter ≥0 | — | `bedrooms` |
| Beds | counter | — | `beds` |
| Bathrooms | counter (step 0.5) | — | `bathrooms` |
| Amenities | 16-checkbox grid (Wifi, Kitchen, Washer, Dryer, Heating, AC, Pool, Hot tub, Free parking, Workspace, TV, Gym, Smoke alarm, First aid, Fire ext, CO alarm) | — | `amenities` |

### Step 4 — Photos
| Field | Input | Required? | Key |
|---|---|---|---|
| Photos (cover, preview flag, sort) | uploader | ≥3 photos | `photos[]` (incl. `original_url`, `filter_preset`, `filter_settings`) |

### Step 5 — Story & rules
| Field | Input | Required? | Key |
|---|---|---|---|
| Title | text, ≤50 chars | ✓ | `title` |
| Listing description | textarea | — | `description` |
| Your property | textarea | — | `propertyOverview` |
| Guest access | textarea | — | `guestAccess` |
| Interaction with guests | textarea | — | `interactionWithGuests` |
| Other details to note | textarea | — | `otherDetails` |
| House rules — booleans | 4 checkboxes (No smoking, No pets, No parties, Quiet hours after 10pm) | — | `houseRules[]` |
| House rules — custom | textarea | — | `customRules` |
| Check-in time | time picker | — | `checkIn` |
| Check-out time | time picker | — | `checkOut` |

### Step 6 — Pricing & booking rules
| Field | Input | Required? | Key |
|---|---|---|---|
| Nightly rate (USD) | number | ✓ (>0) | `price` |
| Cleaning fee | number | — | `cleaningFee` |
| Weekly discount (%) | number 0–99 | — | `weeklyDiscount` |
| Monthly discount (%) | number 0–99 | — | `monthlyDiscount` |
| Min nights | number | — | `minNights` |
| Advance notice (days) | number | — | `advanceNoticeDays` |
| Prep days between bookings | number | — | `prepDays` |
| Default availability | radio: Available / Unavailable / Possibly | — | `defaultAvailability` |

### Step 7 — Listing preview toggles
| Field | Input | Default | Key |
|---|---|---|---|
| Show title in preview | switch | on | `previewShowTitle` |
| Show price range | switch | on | `previewShowPriceRange` |
| Show description | switch | on | `previewShowDescription` |
| → Use preview-specific description | nested switch | off | `usePreviewSpecificDescription` |
| Preview-specific description | textarea, ≤200 chars | — | `previewDescription` |
| Show host first name | switch | on | `previewShowHostFirstName` |
| Show profile photo | switch | on | `previewShowProfilePhoto` |
| Show neighborhood | switch | on | `previewShowNeighborhood` |
| Show approximate map area | switch | on | `previewShowMapArea` |
| Show rating + review count | switch | on | `previewShowRating` |
| Show amenities | switch | on | `previewShowAmenities` |
| Show bed/bath counts | switch | on | `previewShowBedCounts` |
| Show house rules | switch | on | `previewShowHouseRules` |

### Step 8 — Visibility & access
| Field | Input | Default | Key |
|---|---|---|---|
| Visibility mode | one-of `public` / `preview_gated` / `hidden` (presets always pick `preview_gated`) | `preview_gated` | `visibilityMode` |
| Preset | Standard / Open network / Private (template only) | — | (not stored — sets the rules below) |
| See Preview rule | type + threshold | `{type:"anyone"}` | `accessSeePreview` |
| Full Listing + Contact rule | type + threshold | `{type:"min_score", threshold:15}` | `accessFullListingContact` |
| Allow intro requests | toggle | on | `allowIntroRequests` |

### Step 9 — Review & publish
No new fields collected. Publishes via `POST /api/listings` then `PATCH /api/listings/{id}/calendar-settings`.

---

## 3. Submit-handler trace

### `POST /api/listings` (create) — `src/app/api/listings/route.ts:7-138`

Body keys it reads: `property_type`, `title`, `area_name`, `description`, `price_min`, `price_max`, `availability_start`, `availability_end`, `availability_flexible`, `house_rules`, `amenities`, `preview_visibility`, `full_visibility`, `min_trust_score`, `specific_user_ids`, `photos`, `visibility_mode`, `preview_description`, `access_settings`.

**Hardcoded by wizard** (legacy shims): `preview_visibility="anyone"`, `full_visibility="anyone"`, `min_trust_score=0`, `specific_user_ids=[]`. **Never sent**: `availability_start`, `availability_end`, `availability_flexible` (default to NULL/false).

After insert, `listing_photos` rows are inserted including filter fields.

### `PATCH /api/listings/[id]` (edit) — `src/app/api/listings/[id]/route.ts:7-86`

Passthrough whitelist: `is_active`, `title`, `description`, `area_name`, `property_type`, `price_min`, `price_max`, `house_rules`, `availability_start`, `availability_end`, `availability_flexible`, `preview_visibility`, `full_visibility`, `min_trust_score`, `visibility_mode`, `preview_description`. Plus arrays `amenities`, `specific_user_ids`, and JSONB `access_settings`. **No `cleaning_fee`, `default_availability_status`, `min_trust_gate`, or any of the calendar-rule columns.**

### `PATCH /api/listings/[id]/calendar-settings` — `src/app/api/listings/[id]/calendar-settings/route.ts:40-51`

Passthrough whitelist: `min_nights`, `max_nights`, `prep_days`, `advance_notice_days`, `availability_window_months`, `checkin_time`, `checkout_time`, `blocked_checkin_days`, `blocked_checkout_days`, `default_availability_status`. Wizard sends only `min_nights`, `advance_notice_days`, `prep_days`, `checkin_time`, `checkout_time`.

### Description-blob serialization — `src/lib/listing-meta.ts`

`encodeListingMeta(meta, body)` returns `<!--meta:${JSON.stringify(meta)}-->\n\n${body}` and stuffs the entire wizard meta object in front of the description text. `parseListingMeta(description)` is called by `browse-data.ts`, `listing-detail-data.ts`, `wishlist-data.ts`, the edit form, and the description-section component. **Every read path goes through this parser** — without it the meta would render as raw HTML comment text.

`ListingMeta` shape: `placeKind`, `propertyLabel`, `guests`, `bedrooms`, `beds`, `bathrooms`, `address{street,city,state,zip,lat,lng}`, `cleaningFee`, `propertyOverview`, `guestAccess`, `interactionWithGuests`, `otherDetails`, `weeklyDiscount`, `monthlyDiscount`, `defaultAvailability`.

---

## 4. Gap matrix (every wizard field)

Status legend: ✓ stored as a real DB column · ⚠ JSONB-blob (description meta) · ⚠* JSONB inside `access_settings.preview_content` · ✗ dropped (collected but never sent) · ❓ unclear

| Step | Wizard field (`WizardState`) | Submit-payload key | Persists to | Status |
|---|---|---|---|---|
| 1 | `placeKind` | (folded into `property_type` via `propertyTypeToDb`) + `meta.placeKind` | `listings.property_type` (lossy) + `description` blob | ⚠ + lossy |
| 1 | `propertyLabel` | `property_type` (mapped) + `meta.propertyLabel` | `listings.property_type` (collapses 6→4) + blob | ⚠ + lossy |
| 2 | `street` | `meta.address.street` | `description` blob | ⚠ |
| 2 | `city` | `meta.address.city` (also seeds `area_name` fallback) | blob (+ `area_name`) | ⚠ |
| 2 | `state` | `meta.address.state` (also seeds `area_name`) | blob (+ `area_name`) | ⚠ |
| 2 | `zip` | `meta.address.zip` | blob | ⚠ |
| 2 | `lat` | `meta.address.lat` | blob | ⚠ |
| 2 | `lng` | `meta.address.lng` | blob | ⚠ |
| 2 | `areaName` | `area_name` | `listings.area_name` | ✓ |
| 3 | `guests` | `meta.guests` | blob | ⚠ |
| 3 | `bedrooms` | `meta.bedrooms` | blob | ⚠ |
| 3 | `beds` | `meta.beds` | blob | ⚠ |
| 3 | `bathrooms` | `meta.bathrooms` | blob | ⚠ |
| 3 | `amenities` | `amenities` | `listings.amenities` (text[]) | ✓ |
| 4 | `photos` | `photos[]` | `listing_photos` (separate table) | ✓ |
| 4 | `photos[i].is_preview` | `photos[i].is_preview` | `listing_photos.is_preview` | ✓ |
| 4 | `photos[i].is_cover` | `photos[i].is_cover` | `listing_photos.is_cover` | ✓ |
| 4 | `photos[i].original_url` | passthrough | `listing_photos.original_url` | ✓ |
| 4 | `photos[i].filter_preset` | passthrough | `listing_photos.filter_preset` | ✓ |
| 4 | `photos[i].filter_settings` | passthrough | `listing_photos.filter_settings` | ✓ |
| 5 | `title` | `title` | `listings.title` | ✓ |
| 5 | `description` | `description` (with meta-blob prefix) | `listings.description` | ✓ |
| 5 | `propertyOverview` | `meta.propertyOverview` | blob | ⚠ |
| 5 | `guestAccess` | `meta.guestAccess` | blob | ⚠ |
| 5 | `interactionWithGuests` | `meta.interactionWithGuests` | blob | ⚠ |
| 5 | `otherDetails` | `meta.otherDetails` | blob | ⚠ |
| 5 | `houseRules[]` (4 booleans) | joined into `house_rules` text | `listings.house_rules` (text) | ✓ (lossy — booleans become free-text lines) |
| 5 | `customRules` | appended to `house_rules` text | `listings.house_rules` | ✓ |
| 5 | `checkIn` | `checkin_time` (calendar-settings PATCH) | `listings.checkin_time` | ✓ |
| 5 | `checkOut` | `checkout_time` (calendar-settings PATCH) | `listings.checkout_time` | ✓ |
| 6 | `price` | `price_min` + `price_max` (same value) | `listings.price_min`, `listings.price_max` | ✓ |
| 6 | `cleaningFee` | `meta.cleaningFee` | blob | ⚠ — **column `listings.cleaning_fee` exists and is read elsewhere but is never written by listing CRUD; lives only in blob** |
| 6 | `weeklyDiscount` | `meta.weeklyDiscount` | blob | ⚠ |
| 6 | `monthlyDiscount` | `meta.monthlyDiscount` | blob | ⚠ |
| 6 | `minNights` | calendar-settings PATCH `min_nights` | `listings.min_nights` | ✓ |
| 6 | `advanceNoticeDays` | calendar-settings PATCH `advance_notice_days` | `listings.advance_notice_days` | ✓ |
| 6 | `prepDays` | calendar-settings PATCH `prep_days` | `listings.prep_days` | ✓ |
| 6 | `defaultAvailability` | `meta.defaultAvailability` | blob | ⚠ — **column `default_availability_status` exists with the right CHECK constraint, but the wizard never sends it; the wizard's value-set (`available`/`unavailable`/`possibly`) also doesn't match the DB CHECK (`available`/`possibly_available`/`blocked`)** |
| 7 | `previewShowTitle` | `access_settings.preview_content.show_title` | `listings.access_settings` jsonb | ⚠* |
| 7 | `previewShowPriceRange` | `…show_price_range` | jsonb | ⚠* |
| 7 | `previewShowDescription` | `…show_description` | jsonb | ⚠* |
| 7 | `usePreviewSpecificDescription` | `…use_preview_specific_description` | jsonb | ⚠* |
| 7 | `previewDescription` | `preview_description` | `listings.preview_description` | ✓ |
| 7 | `previewShowHostFirstName` | `…show_host_first_name` | jsonb | ⚠* |
| 7 | `previewShowProfilePhoto` | `…show_profile_photo` | jsonb | ⚠* |
| 7 | `previewShowNeighborhood` | `…show_neighborhood` | jsonb | ⚠* |
| 7 | `previewShowMapArea` | `…show_map_area` | jsonb | ⚠* |
| 7 | `previewShowRating` | `…show_rating` | jsonb | ⚠* |
| 7 | `previewShowAmenities` | `…show_amenities` | jsonb | ⚠* |
| 7 | `previewShowBedCounts` | `…show_bed_counts` | jsonb | ⚠* |
| 7 | `previewShowHouseRules` | `…show_house_rules` | jsonb | ⚠* |
| 8 | `visibilityMode` | `visibility_mode` | `listings.visibility_mode` | ✓ (only `preview_gated` ever set) |
| 8 | `accessSeePreview` | `access_settings.see_preview` | jsonb | ⚠* |
| 8 | `accessFullListingContact` | `access_settings.full_listing_contact` (clamped vs. see_preview) | jsonb | ⚠* |
| 8 | `allowIntroRequests` | `access_settings.allow_intro_requests` | jsonb | ⚠* |

**Summary**: ~13 fields land in real columns; ~16 land in the description meta-blob; ~16 land inside `access_settings` JSONB (legitimately, that's what the column is for); 0 field is *fully* dropped (the meta-blob captures what the schema doesn't). However several wizard fields are silently disconnected from the matching real column (`cleaning_fee`, `default_availability_status`).

---

## 5. Orphan columns (confirmed)

Definition: a column that has **no live writer** *and/or* whose readers are dead code. Verified by reading every grep hit across `src/`.

| Column | Type | Status | Evidence |
|---|---|---|---|
| `address` (text) | YES NULL | **Orphan, drop candidate (low risk)** | Zero reads (`grep -rn "row\.address\|listing\.address"` → 0 hits in active code; structured address lives in `meta.address` blob). Never written by wizard or any API. |
| `price_per_night` | numeric NULL | **Orphan, drop candidate (low risk)** | Zero reads, zero writes. Wizard uses `price_min`/`price_max`. The `custom_price_per_night` references in code are on `listing_availability`, not `listings`. |
| `preview_photos` | jsonb NULL | **Orphan, drop candidate (low risk)** | Zero references in source. Preview/cover state lives on `listing_photos.is_preview` / `is_cover`. |
| `availability_start` | date NULL | **Vestigial** — read into `ListingDetail` shape but no UI consumer | Real availability now lives in `listing_availability` table. |
| `availability_end` | date NULL | **Vestigial** — same | |
| `availability_flexible` | boolean NULL | **Orphan** — only typed in dead `listing-data.ts` | Wizard never writes it (defaults to false). |
| `min_trust_gate` | int NOT NULL DEFAULT 0 | **Orphan-write** — has readers but **zero writers**. Every row stays at 0; the partial index `idx_listings_min_trust_gate` is therefore empty. Consumers (`browse-data`, `wishlist-data`, `listing-detail-data`, `trust-gate.tsx`, `gated-listing-dialog.tsx`) all `?? 0` and proceed. | Decision needed: keep + wire up writes, or drop the column + delete the dead readers. |
| `preview_visibility` | text NULL DEFAULT `'anyone'` | **Legacy** — wizard hardcodes `"anyone"`; consumed only by `src/lib/listing-data.ts` whose function exports are never imported by runtime code (only types). | Drop after confirming `listing-data.ts` is deletable. |
| `full_visibility` | text NULL DEFAULT `'vouched'` | **Legacy** — same | |
| `min_trust_score` | int NULL DEFAULT 0 | **Legacy** — same | |
| `specific_user_ids` | uuid[] NULL DEFAULT `'{}'` | **Legacy** — same. Access lists now live in `access_settings.see_preview.user_ids` etc. | |

`src/lib/listing-data.ts` has only TYPE imports remaining (`ListingPhoto`, `ListingRow`); its two function exports (`getListingsForViewer`, `getListingForViewer`) are not imported anywhere in `src/`. The runtime data layer is `browse-data.ts`, `listing-detail-data.ts`, `wishlist-data.ts`. Cleanup of the four legacy text-enum columns is a `listing-data.ts` deletion away.

### Not orphans (kept for clarity)
- `cleaning_fee` is read in 6+ places (listing detail page, messaging-data, contact-requests, booking flows). The orphan side is the **wizard write**, not the column.
- `default_availability_status` is read by `/api/listings/[id]/availability` route. The orphan side is the wizard write.
- `cancellation_policy_override` — actively managed by dedicated routes.
- `avg_listing_rating`, `listing_review_count` — written by stay-confirmation review triggers.
- `visibility_mode` — written by both create + edit; read on detail page + browse + proposals.
- `is_active` — written by edit + delete; read everywhere.
- `min_nights`, `max_nights`, `prep_days`, `advance_notice_days`, `availability_window_months`, `blocked_checkin_days`, `blocked_checkout_days`, `checkin_time`, `checkout_time` — all read by `validate-booking-dates.ts` and listing-detail; written by calendar-settings PATCH.

---

## 6. Captured-ideas gap status

| Idea (from session prompt + Plan) | Present? | Evidence |
|---|---|---|
| `stay_style` enum (`vacation_rental` / `lived_in_home` / `partial_prep`) | **MISSING** | No column matches; not in wizard, not in API, not in any migration. |
| `service_discounts` (guest does laundry/turnover/cleaning for discount) | **MISSING** | No column, no wizard input. |
| Structured pet-policy fields | **MISSING** | Pet policy collapses into a `houseRules[]` boolean ("No pets") + the free-text `customRules`. No structured columns; no JSONB sub-object. |
| First-class `max_guests`, `bedrooms`, `beds`, `bathrooms` columns | **MISSING** | Lives in `description` meta-blob only. |
| First-class `cleaning_fee` write path | Column exists, **writer missing** | Wizard sends to meta-blob; column readers exist. Mismatch — see Open Questions §1. |
| `weekly_discount` / `monthly_discount` | **MISSING** | Lives in meta-blob only. |
| `default_availability_status` write path | Column exists with CHECK, **wizard never writes** | Mismatched value sets (wizard `available`/`unavailable`/`possibly` vs. column `available`/`possibly_available`/`blocked`). See Open Questions §2. |
| Structured address columns (`street`, `city`, `state`, `zip`, `lat`, `lng`) | **MISSING** | Address lives in meta-blob; only `area_name` (free text) is a real column. |

---

## 7. Stale defaults & shape drift

### 7.1 `access_settings` column DEFAULT is stale

**Live column default** (from information_schema):

```json
{
  "message":            { "type": "min_score", "threshold": 10 },
  "see_full":           { "type": "min_score", "threshold": 10 },
  "see_preview":        { "type": "anyone" },
  "request_book":       { "type": "min_score", "threshold": 20 },
  "request_intro":      { "type": "anyone" },
  "view_host_profile":  { "type": "anyone" }
}
```

This is the pre-019 **6-key shape**. Migrations 019 + 020 collapsed it to the **4-key shape** in every existing row but **did not change the column DEFAULT**:

```json
{
  "see_preview":           { "type": "anyone" },
  "full_listing_contact":  { ... },
  "allow_intro_requests":  true,
  "preview_content":       { "show_title": true, "show_price_range": true, ... }
}
```

**Impact**: any future `INSERT INTO listings(...)` that omits `access_settings` will land with the legacy 6-key shape. The trust normalizer in `src/lib/trust/types.ts` apparently tolerates both, but new rows would diverge from the seed/migrated shape. Easy fix in mig 045: `ALTER COLUMN access_settings SET DEFAULT '{...new shape...}'::jsonb`.

### 7.2 Other JSONB column defaults — clean
- `preview_photos` default = NULL (column is orphan anyway).
- `cancellation_policy_override` default = NULL (intentional — fall-through to host default).
- `specific_user_ids` default = `'{}'::uuid[]` (legacy column; will go away).

### 7.3 `property_type` CHECK vs UI mismatch

UI labels: House · Apartment · Condo · Townhouse · Cabin · Other. DB CHECK: only `apartment`/`house`/`room`/`other`. `propertyTypeToDb()` collapses Condo→apartment and Townhouse/Cabin→house, and folds the place-kind into `room` for Private/Shared. Detail is preserved only inside `meta.propertyLabel` — which means filter/sort by "Cabin" against the real column is impossible.

### 7.4 `default_availability_status` CHECK vs wizard mismatch

Wizard collects `available` / `unavailable` / `possibly`. Column CHECK: `available` / `possibly_available` / `blocked`. The wizard never sends this, so the mismatch never reaches the DB — but if we wire up the write (recommended), the value mapping needs to be `possibly`→`possibly_available` and `unavailable`→`blocked`.

---

## 8. Recommended migration 045 changes (locked spec for S10.5)

This is the section S10.5 consumes. Section 8 is intentionally copy-pasteable.

### 8.1 ADD columns (promote out of meta-blob)

| Column | Type | Default | NULL? | Rationale |
|---|---|---|---|---|
| `place_kind` | `text` | NULL | YES | CHECK in `('entire','private','shared')`. Currently in `meta.placeKind`. Used by browse filters & display ("Entire home"). |
| `property_label` | `text` | NULL | YES | The 6-value UI label (House/Apartment/Condo/Townhouse/Cabin/Other). New CHECK list. Keeps `property_type` (4 values) as the coarse bucket for back-compat; `property_label` is the precise UI value. |
| `max_guests` | `integer` | NULL | YES | CHECK ≥ 1. Currently `meta.guests`. Needed for browse-side guest-count filter. |
| `bedrooms` | `integer` | NULL | YES | CHECK ≥ 0. |
| `beds` | `integer` | NULL | YES | CHECK ≥ 0. |
| `bathrooms` | `numeric(3,1)` | NULL | YES | CHECK ≥ 0. Half-bath supported (UI uses 0.5 step). |
| `street` | `text` | NULL | YES | Currently `meta.address.street`. |
| `city` | `text` | NULL | YES | |
| `region` | `text` | NULL | YES | "state" is a reserved-feeling name in some ORMs; `region` is safer. (Or keep `state` — see Open Question §3.) |
| `postal_code` | `text` | NULL | YES | |
| `lat` | `numeric(9,6)` | NULL | YES | |
| `lng` | `numeric(9,6)` | NULL | YES | |
| `weekly_discount_pct` | `integer` | NULL | YES | CHECK 0–99. Currently `meta.weeklyDiscount`. |
| `monthly_discount_pct` | `integer` | NULL | YES | CHECK 0–99. Currently `meta.monthlyDiscount`. |
| `extended_overview` | `text` | NULL | YES | Currently `meta.propertyOverview`. ("Your property" textarea on Step 5.) |
| `guest_access_text` | `text` | NULL | YES | Currently `meta.guestAccess`. |
| `interaction_text` | `text` | NULL | YES | Currently `meta.interactionWithGuests`. |
| `other_details_text` | `text` | NULL | YES | Currently `meta.otherDetails`. |

Plus, optionally:
| Column | Type | Default | NULL? | Rationale |
|---|---|---|---|---|
| `preview_settings` | `jsonb` | `'{}'::jsonb` | NO | Move the per-section preview toggles out of `access_settings.preview_content` into a dedicated column. Keeps `access_settings` purely about *who can see what*; preview_settings is *what gets shown*. (Open Question §4 — this is a structural decision Loren should weigh in on.) |

### 8.2 DROP columns

| Column | Reason | Risk |
|---|---|---|
| `address` (text) | Zero reads, zero writes. Replaced by `street`/`city`/`region`/`postal_code`. | low |
| `price_per_night` | Zero reads, zero writes. `price_min`/`price_max` superseded it. | low |
| `preview_photos` | Zero references; preview state lives on `listing_photos`. | low |
| `availability_start` | Vestigial read-only field; `listing_availability` table is canonical. | low |
| `availability_end` | Same. | low |
| `availability_flexible` | Write-only with no readers. | low |
| `preview_visibility` | Legacy text-enum; only consumed by dead `listing-data.ts`. | low (after deleting `listing-data.ts` exports) |
| `full_visibility` | Same. | low |
| `min_trust_score` | Same. | low |
| `specific_user_ids` | Legacy; `access_settings.see_preview.user_ids` is canonical. | low |
| `min_trust_gate` | No writers anywhere. | **medium** — readers exist (`browse-data`, `wishlist-data`, `listing-detail-data`, `trust-gate.tsx`, `gated-listing-dialog.tsx`); they all default to 0 already, but the dialog's gate-display logic + the partial index reference the column. Drop requires deleting the readers in the same PR. (Open Question §5 — defer or yank?) |

All drops listed are mechanically safe given current code; the *coordination* with `listing-data.ts` deletion is what makes them medium-effort instead of trivial.

### 8.3 JSONB defaults to FIX

`access_settings`:
```sql
ALTER TABLE listings
  ALTER COLUMN access_settings SET DEFAULT
  '{"see_preview":{"type":"anyone"},
    "full_listing_contact":{"type":"min_score","threshold":15},
    "allow_intro_requests":true,
    "preview_content":{"show_title":true,"show_price_range":true,"show_description":true,"show_host_first_name":true,"show_profile_photo":true,"show_neighborhood":true,"show_map_area":true,"show_rating":true,"show_amenities":true,"show_bed_counts":true,"show_house_rules":true,"use_preview_specific_description":false}}'::jsonb;
```

If §8.1's optional `preview_settings` JSONB is added, also set ITS default to the all-true shape, and drop the `preview_content` sub-object from `access_settings` default.

### 8.4 Backfill from `description` meta-blob into new columns

Because every existing listing's bedrooms/beds/etc lives inside `<!--meta:{...}-->`, mig 045 should:

1. Use a PL/pgSQL block to extract the meta JSON from `description` (regex `^<!--meta:(\{.*?\})-->`).
2. Populate the new columns from the parsed JSON (`max_guests`, `bedrooms`, `beds`, `bathrooms`, address parts, `weekly_discount_pct`, `monthly_discount_pct`, `extended_overview`, etc.).
3. Strip the meta-comment from `description` so reads no longer need `parseListingMeta`.

This is the most-error-prone step. Recommend writing it in TypeScript in a one-off seed-style script that reads + writes via the service role rather than as raw PL/pgSQL — easier to test and to bail out on parse failures.

### 8.5 Wizard / API rewires (not part of mig 045 itself, but the next-session work)

Once mig 045 is in:
- `POST /api/listings` body must accept `place_kind`, `property_label`, `max_guests`, `bedrooms`, `beds`, `bathrooms`, address parts, `cleaning_fee`, `weekly_discount_pct`, `monthly_discount_pct`, `default_availability_status`, the four extended-description fields, and `preview_settings` (if split out).
- `PATCH /api/listings/[id]` whitelist needs the same additions.
- `src/lib/listing-meta.ts` becomes a read-only fallback for legacy rows (or is deleted entirely after backfill verification).
- `src/lib/listing-data.ts` function exports (`getListingsForViewer`, `getListingForViewer`) and the four legacy-column types should be deleted.

---

## 9. Open questions for Loren

1. **`cleaning_fee` write path.** The column exists and is read in 6+ places (booking flows include it in totals). The wizard pretends to collect it but only stuffs it in the meta-blob. Existing rows therefore have NULL `cleaning_fee` and a `meta.cleaningFee`. **Decision: in mig 045 backfill, copy `meta.cleaningFee → cleaning_fee` and rewire the wizard to write the column directly?** (Recommend yes.)

2. **`default_availability_status` value set.** Wizard offers Available / Unavailable / Possibly. Column CHECK is `available` / `possibly_available` / `blocked`. **Decision: rename wizard values to match column (preferred), or change the column CHECK to match the wizard?** (Recommend renaming wizard.)

3. **Reserved word risk: `state`.** I proposed `region` to avoid PostgreSQL/ORM friction. Some teams keep `state` and quote it. **Pick: `state` or `region`?** Either works, just be consistent.

4. **Preview toggles — split out or leave in `access_settings`?**
   - **A (split)**: new `preview_settings jsonb` column. Cleaner separation: `access_settings` = WHO, `preview_settings` = WHAT. Easier RLS reasoning later.
   - **B (leave)**: keep them inside `access_settings.preview_content`. No migration churn; current code works.
   Recommend A, but it's a judgment call.

5. **`min_trust_gate` — drop, or wire up?**
   - **A (drop)**: yank the column + delete the 5 readers in the same migration PR. The column has been at 0 for every row for the entire history of mig 013 — the gate-display code is dead.
   - **B (defer)**: keep until a future "trust-gate as a feature" session decides whether to revive it. Adds debt; column stays at 0.
   Recommend A.

6. **Place type → `property_type` collapse.** The 4-value CHECK forces Condo/Townhouse/Cabin into apartment/house/house. If we add `property_label` (6-value), do we want to **also widen `property_type`** to match the UI, or **drop `property_type` entirely** in favor of `property_label`? Affects browse filtering.

7. **Drop legacy access columns in 045 or defer to a cleanup-only mig?** `preview_visibility`, `full_visibility`, `min_trust_score`, `specific_user_ids` are all legacy text-enum + listing-data.ts. Bundling them into 045 means coordinating a `listing-data.ts` deletion in the same PR. Splitting them out keeps 045 additive-only. Recommend bundle (less ceremony at alpha scale).

8. **`address` text column — drop unconditionally?** Zero readers + zero writers found. Confidence high, but flagging to confirm before droppage.

9. **House rules: keep as `text`, or normalize to a structured blob?** The wizard collects 4 booleans (no_smoking, no_pets, no_parties, quiet_hours) + custom text and joins them with `\n`. Browse-side filtering on "no pets" is impossible against a text blob. Out of scope for 045 (would touch UI), but worth deciding before wiring up filters.

---

*End of audit. S10.5 should treat §8 as the locked spec; §9 needs Loren's input before §8 hardens.*
